
var statistics = {
        "noOfDem": 0,
        "noOfRep": 0,
        "noOfInd": 0
};

onload = (function(){ 

    const allMembers = data.results[0].members;

    countPartyMembers();
});

function countPartyMembers(myArray){

    // myArray.forEach(member => );

}